export enum PagesPaths {
  CHARACTERS = "/characters",
  HOME = "/",
  MOVIE = "/movie",
  PLANETS = "/planets",
  WILDCARD = "*", // This is a catch-all route
}

export enum DataEndpoints {
  FILMS = "/films",
  PEOPLE = "/people",
  PLANETS = "/planets",
  SPECIES = "/species",
  STARSHIPS = "/starships",
  VEHICLES = "/vehicles",
}
